asgiref==3.9.1
Django==5.2.4
sqlparse==0.5.3
tzdata==2025.2

# Image handling for user profile photos
Pillow>=10.0.0

# HTTPS and Security Enhancements
django-csp>=3.7                    # Content Security Policy middleware
django-environ>=0.11.0             # Environment variable management for secure configuration
python-dotenv>=1.0.0               # Load environment variables from .env files

# SSL/TLS Security
cryptography>=41.0.0               # Strong cryptographic support
pyOpenSSL>=23.0.0                  # SSL/TLS utilities and certificate handling

# Security Headers and Middleware
django-axes>=6.0.0                 # Brute-force protection (recommended)

# Logging and Monitoring
django-structlog>=4.0.0            # Structured logging for security events (optional)

# Production Database Support (recommended for production)
psycopg2-binary>=2.9.0             # PostgreSQL adapter
# mysqlclient>=2.1.0               # MySQL adapter (alternative)

# Production Web Server Support
gunicorn>=21.0.0                   # WSGI HTTP Server
whitenoise>=6.5.0                  # Static file serving with security headers

# Development and Security Testing Tools (dev dependencies)
django-debug-toolbar>=4.0.0       # Development debugging (DEBUG=True only)
bandit>=1.7.5                     # Security linting tool
safety>=2.3.0                     # Dependency vulnerability scanner

# Optional: Advanced Security Features
# django-two-factor-auth>=1.15.0  # Two-factor authentication
# django-ratelimit>=4.0.0         # Rate limiting middleware  
# django-cors-headers>=4.0.0      # CORS handling if needed for APIs

# Testing and Quality Assurance
coverage>=7.0.0                   # Code coverage analysis
pytest-django>=4.5.0              # Testing framework
pytest-cov>=4.0.0                 # Coverage reporting for pytest

# Code Quality and Security
flake8>=6.0.0                     # Code linting
black>=23.0.0                     # Code formatting
isort>=5.12.0                     # Import sorting

# Documentation
Sphinx>=7.0.0                     # Documentation generation (optional)
sphinx-rtd-theme>=1.3.0           # Documentation theme (optional)

# Production Deployment Utilities
redis>=4.5.0                      # Caching and session storage (recommended)
celery>=5.3.0                     # Background task processing (optional)

# Notes for Production Deployment:
# 1. Remove django-debug-toolbar in production
# 2. Consider using specific versions instead of >= for production stability
# 3. Add database-specific packages (psycopg2-binary for PostgreSQL)
# 4. Include monitoring packages (sentry-sdk, django-prometheus) as needed
# 5. Use virtual environments and pip-tools for dependency management

# Security Package Descriptions:
# - django-csp: Implements Content Security Policy headers
# - django-environ: Secure environment variable management
# - cryptography: Core cryptographic functions for HTTPS
# - django-axes: Protects against brute-force attacks
# - bandit: Static security analysis for Python code
# - safety: Checks for known security vulnerabilities in dependencies